import { MessagebytypePipe } from './messagebytype.pipe';

describe('MessagebytypePipe', () => {
  it('create an instance', () => {
    const pipe = new MessagebytypePipe();
    expect(pipe).toBeTruthy();
  });
});
