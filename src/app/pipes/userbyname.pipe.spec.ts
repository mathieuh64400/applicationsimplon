import { UserbynamePipe } from './userbyname.pipe';

describe('UserbynamePipe', () => {
  it('create an instance', () => {
    const pipe = new UserbynamePipe();
    expect(pipe).toBeTruthy();
  });
});
