import { ProductbycategoriePipe } from './productbycategorie.pipe';

describe('ProductbycategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductbycategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
